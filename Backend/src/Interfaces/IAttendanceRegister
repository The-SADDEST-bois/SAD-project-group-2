import { Schema } from "mongoose";

export interface IAttendanceRegister {
    _id?: Schema.Types.ObjectId;
    attendance:
    [
        {
            _id?: Schema.Types.ObjectId,
            firstName: string,
            lastName: string,
            attendend: number
        }
    ];
  }